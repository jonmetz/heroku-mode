1. Loading twenty functions into the global emacs namespace shouldn't be what this does when it is loaded.
A few important functions should be loaded, and the rest should only be loaded once heroku-mode is activated.
2. Certain functions like fork take arguments on the commandline tool but don't accept them now.
